export default function ProjectOverviewPage({
  params,
}: {
  params: {
    id: string;
  };
}) {
  const projectId = params.id;

  
  return (
    <>
      Project
      <span></span>
    </>
  );
}
