{
  "name": "pixeleye",
  "scripts": {
    "version:npm": "changeset version && pnpm install --no-frozen-lockfile",
    "publish:npm": "turbo build --filter=pixeleye --filter=puppeteer --filter=playwright --filter=cypress --filter=storybook && changeset publish"
  },
  "license": "AGPL-3.0",
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.3",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^1.13.3",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@8.10.5"
}